import type { LevelRange, ManaProfile } from "../../types/shared";

export const DEFAULT_LEVEL_RANGE: LevelRange = {
  min: 1,
  max: 100,
};

export const DEFAULT_MANA_PROFILE: ManaProfile = {
  start: 150,
  max: 600,
};
